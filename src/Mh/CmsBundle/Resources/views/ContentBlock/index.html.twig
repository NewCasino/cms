<h1>ContentBlock list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Content_block_content</th>
            <th>Content_block_rank</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('content-blocks_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.contentblockcontent }}</td>
            <td>{{ entity.contentblockrank }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('content-blocks_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('content-blocks_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('content-blocks_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
