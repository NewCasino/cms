<h1>ContentBlock</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Content_block_content</th>
            <td>{{ entity.contentblockcontent }}</td>
        </tr>
        <tr>
            <th>Content_block_rank</th>
            <td>{{ entity.contentblockrank }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('content-blocks') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('content-blocks_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('content-blocks_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
